Task: Create a new React application using Create React App. Implement a React component called ListRenderer that renders a list of items using JSX.

1. Define an array of items, for example, ["Apple", "Banana", "Orange"].
2. Render an unordered list (<ul>) using JSX.
3. Map through the array of items and render a list item (<li>) for each item in the array.
4. Ensure each list item displays the corresponding item from the array.
5. Test your React app to verify that the list is rendered correctly.

Submission Guidelines:

1. Set up a new React application using Create React App.
2. Create a new file named ListRenderer.jsx in the src/components directory.
3. Define the ListRenderer component in the ListRenderer.jsx file.
4. Implement the logic to render a list of items using JSX and the array provided.
5. Ensure your React app runs without errors and displays the ListRenderer component correctly.
